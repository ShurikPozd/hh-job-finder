services:
  bot:
    build: .
    container_name: hh-job-finder
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data: